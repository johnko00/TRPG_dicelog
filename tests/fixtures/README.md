# Fixtures

`representative-messages.json` and `anonymous-ccfolia.html` contain only
anonymized, minimal examples for parser tests. Full exports from real sessions
must stay under `tests/fixtures/local/` (ignored by Git) and must not be
committed to a public repository.
